# SonicStar Topic Manager

Admits SonicStar Song Source Protocol (`sssp`) outputs into the
`tm_sonicstar` topic. Each admissible output is a single `OP_RETURN`
followed by a UTF-8 JSON push describing the song.

Full documentation is fleshed out in step 5 of the implementation plan.
