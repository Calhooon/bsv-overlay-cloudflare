/target
.DS_Store
*.swp
*.swo
.env
.dev.vars
node_modules/
secrets.md
# Wrangler local dev state (local D1 SQLite, caches, etc.)
.wrangler/
# Docker volumes + container state (reference stack)
reference/.docker/
# Third-party TS reference material (sonicstar plugin source-of-truth from
# sonicstar.net). Used as design reference; not ours to redistribute.
ruth/
