/target
.DS_Store
*.swp
*.swo
.env
.dev.vars
node_modules/
secrets.md
# Wrangler local dev state (local D1 SQLite, caches, etc.)
.wrangler/
# Docker volumes + container state (reference stack)
reference/.docker/

# rendered deploy configs (ids injected from the private secrets store; never commit)
wrangler.deploy.toml
wrangler.low.deploy.toml
